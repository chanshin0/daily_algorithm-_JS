function solution(numbers, num1, num2) {
    return numbers.slice(num1, num2+1);
    // let answer = []
    // for (let i=0; i<numbers.length; i++){
    //     const num = numbers[i]
    //     if (num1<=i && i<=num2) answer.push(num)
    // }
    // return answer
}